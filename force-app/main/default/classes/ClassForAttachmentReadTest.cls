@isTest
public class ClassForAttachmentReadTest {

    @istest
    Public static  void testAttachmentLog() {
        test.startTest();
        //Account acc=[select id from Account limit 1];
  		ClassForAttachmentRead obj= new ClassForAttachmentRead();    
        test.stoptest();
    }
    
    
}